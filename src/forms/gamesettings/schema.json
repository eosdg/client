{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "maxSips": {
      "title": "Maximale Anzahl an Schlucken",
      "type": "number",
      "minimum": 0,
      "description": "Wenn du das leer lässt, bist du der Gnade der Fragensteller ausgesetzt"
    },
    "kategorien": {
      "type": "array",
      "enum": []
    }
  },
  "required": [
  ]
}
