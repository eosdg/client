{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "maxSips": {
      "title": "Maximale Anzahl an Schlucken",
      "type": "number",
      "minimum": 0,
      "description": "Wenn du das leer lässt, bist du der Gnade der Fragensteller ausgesetzt"
    },
    "kategorien": {
      "type": "array",
      "enum": []
    },
    "zeitlimit": {
      "title": "Zeitlimit pro Frage in Sekunden",
      "type": "number",
      "default": 20,
      "minimum": 5
    }
  },
  "required": [
    "zeitlimit"
  ]
}
